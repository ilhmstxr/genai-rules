Aturan:

Wajib menggunakan readonly class (PHP 8.2+).

Menggunakan Constructor Property Promotion.

Memiliki static method fromRequest($request) untuk pemetaan otomatis dari data input.

Dilarang keras menggunakan properti tunggal public array $data. Semua properti input harus didefinisikan satu per satu dengan tipe data (string, int, bool, dsb) di dalam constructor